<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssjj.androidmvpdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />


    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="com.ssjj.androidmvpdemo.test" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <uses-library android:name="android.test.runner" /> -->
        <!-- <activity android:name="com.ssjj.androidmvpsample.test.Sample" android:label="@string/app_name"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity android:name=".now.MusicActivity" />
        <activity
            android:name=".VideoRecordActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".words.OpenGLActivity"
            android:label="@string/title_activity_open_gl"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".opengl.OpenGLES20Activity"
            android:label="@string/title_activity_open_gl"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>